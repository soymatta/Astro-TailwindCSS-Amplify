---
interface Props {
  name: string;
  description: string;
  img: string;
  repo: string;
  link: string;
  tool_1: string;
  tool_2: string;
  tool_3: string;
}

const { name, description, img, link, repo, tool_1, tool_2, tool_3 } =
  Astro.props;
---
